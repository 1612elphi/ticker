services:
  ticker:
    build: .
    restart: unless-stopped
    network_mode: host
    environment:
      - PORT=${PORT:-3420}
      - PRINTER_HOST=${PRINTER_HOST:-192.168.1.200}
      - PRINTER_PORT=${PRINTER_PORT:-9100}
